/* ═══════════════════════════════════════════════════════
   Bills Calculator TUI — main.tcss
   Theme: Dark terminal, gemini-cli / claude-code style
   ═══════════════════════════════════════════════════════ */

/* ── Global ──────────────────────────────────────────── */
Screen {
    background: #282C34;
    color: #ABB2BF;
    layers: base overlay;
}

Header {
    background: #282C34;
    color: #61AFEF;
    border-bottom: solid #3E4451;
}

Footer {
    background: #282C34;
    color: #5C6370;
    border-top: solid #3E4451;
}

/* ── Content area ────────────────────────────────────── */
#content-area {
    height: 1fr;
    padding: 0 2;
    overflow-y: auto;
    scrollbar-color: #3E4451 #282C34;
    scrollbar-size: 1 1;
}

/* ── Figlet hero ─────────────────────────────────────── */
#hero {
    height: auto;
    margin: 1 0;
    padding: 0;
}

/* ── RichLog output area ─────────────────────────────── */
#output-log {
    height: auto;
    min-height: 10;
    margin-bottom: 1;
    padding: 0;
    scrollbar-color: #3E4451 #282C34;
}

/* ── Command bar (docked bottom, above Footer) ───────── */
CommandInput {
    dock: bottom;
    height: auto;
    min-height: 3;
    background: #282C34;
    border-top: solid #4B5263;
    padding: 0;
}

CommandInput #cmd-prefix {
    width: 3;
    color: #98C379;
    text-style: bold;
}

CommandInput Input {
    background: #282C34;
    border: none;
    height: 1;
    width: 1fr;
    color: #ABB2BF;
}

CommandInput Input:focus {
    border: none;
    background: #282C34;
}

/* ── History screen ──────────────────────────────────── */
HistoryScreen {
    background: #282C34;
}

#history-root {
    height: 1fr;
    padding: 1 2;
}

#history-title {
    text-style: bold;
    color: #61AFEF;
    margin-bottom: 1;
}

#history-table {
    height: 1fr;
    margin-bottom: 1;
    border: round #3E4451;
    background: #282C34;
}

#history-hint {
    color: #5C6370;
}
